# ✅ تحديث تلقائي لبيانات المواطن

## 🎯 الميزة الجديدة

عند تقديم طلب جديد، إذا قام المواطن بتحميل بياناته باستخدام رمز الدخول، ثم قام بتعديل أي من معلوماته الشخصية (الاسم، الهاتف، البريد، الرقم الوطني، العنوان)، سيتم **حفظ التعديلات تلقائياً** عند الضغط على زر "التالي ←".

---

## 📋 آلية العمل

### 1️⃣ المواطن يدخل رمز الدخول
```
TKT-A3B7K
```
- يتم جلب بياناته من قاعدة البيانات
- يتم ملء جميع الحقول تلقائياً

### 2️⃣ المواطن يعدل معلوماته
مثال:
- **رقم الهاتف القديم:** `03495685`
- **رقم الهاتف الجديد:** `03123456`

### 3️⃣ الضغط على "التالي ←"
- النظام يرسل طلب AJAX إلى `update_citizen_data.php`
- يتم تحديث البيانات في جدول `citizens_accounts`
- يتم تحديث `localStorage` أيضاً للمرات القادمة
- يتم الانتقال للخطوة التالية

### 4️⃣ في حال حدوث خطأ
- يظهر تحذير: "حدث خطأ أثناء حفظ التعديلات"
- لكن **النظام يستمر** في الانتقال للخطوة التالية
- البيانات الجديدة تُستخدم في الطلب الحالي

---

## 🗂️ الملفات المعدّلة

### 1. `public/update_citizen_data.php` (جديد)
- يستقبل JSON مع رمز الدخول والبيانات الجديدة
- يحدّث جدول `citizens_accounts`
- يرجع JSON بنتيجة العملية

### 2. `public/citizen-requests.php`
- تم تحويل `nextStep()` إلى `async function`
- تم إضافة استدعاء `updateCitizenData()` عند الانتقال من الخطوة 1
- تم إضافة دالة `updateCitizenData()` الجديدة

---

## 🧪 اختبار الميزة

### الخطوات:
1. افتح: `http://localhost:8080/tekrit_municipality/public/citizen-requests.php`
2. أدخل رمز دخول موجود: `TKT-A3B7K`
3. اضغط "🔍 جلب البيانات"
4. عدّل أي معلومة (مثلاً: رقم الهاتف أو البريد)
5. اضغط "التالي ←"
6. تحقق من `console.log` في المتصفح:
   ```
   ✅ تم تحديث بيانات المواطن بنجاح
   ```
7. ارجع لصفحة حسابات المواطنين للتأكد من التحديث:
   ```
   http://localhost:8080/tekrit_municipality/modules/citizens_accounts.php
   ```

---

## 📊 قاعدة البيانات

### الجدول المحدّث: `citizens_accounts`

```sql
UPDATE citizens_accounts 
SET 
    full_name = ?,
    phone = ?,
    email = ?,
    national_id = ?,
    address = ?
WHERE citizen_id = ?
```

---

## ✨ مزايا إضافية

### 🔒 الأمان
- يتم التحقق من صحة رمز الدخول قبل التحديث
- لا يمكن تحديث بيانات مواطن آخر

### 💾 التخزين المحلي
- يتم تحديث `localStorage` أيضاً
- في المرات القادمة، حتى بدون رمز دخول، ستظهر البيانات المحدثة

### 📝 السجلات
- يتم تسجيل كل تحديث في `error_log`
- يمكن تتبع التعديلات للمراجعة

---

## 🎉 الآن النظام جاهز!

✅ **المواطن لا يحتاج لتحديث بياناته يدوياً في حسابه**  
✅ **التحديث يتم تلقائياً عند تقديم طلب جديد**  
✅ **البيانات المحدثة تُستخدم في جميع الطلبات المستقبلية**

---

**تاريخ الإضافة:** 2025-11-12  
**الحالة:** ✅ جاهز ومُختبر

