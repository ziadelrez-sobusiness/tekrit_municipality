# تقرير تحليل مشروع بلدية تكريت - عكار

## 1. نظرة عامة على المشروع
المشروع عبارة عن نظام إدارة متكامل لبلدية تكريت في عكار، شمال لبنان. يهدف النظام إلى أتمتة العمليات البلدية وتسهيل التواصل مع المواطنين. ينقسم النظام إلى جزأين رئيسيين:
1. **الواجهة الأمامية (Frontend)**: بوابة إلكترونية للمواطنين لتقديم الطلبات والشكاوى ومتابعتها.
2. **الواجهة الخلفية (Backend)**: لوحة تحكم للموظفين لإدارة الأقسام، اللجان، القرارات، والبيانات المالية والإدارية.

## 2. هيكلية المشروع (Project Structure)

### المجلدات الرئيسية:
- **`public/`**: يحتوي على ملفات الواجهة الأمامية التي يراها المواطن (مثل `citizen-dashboard.php`, `citizen-requests.php`).
- **`modules/`**: يحتوي على وحدات النظام الإداري (Backend) مثل إدارة الموظفين (`hr.php`)، المالية (`finance.php`)، والبلدية (`municipality_management.php`).
- **`includes/`**: يحتوي على ملفات المساعدة والخدمات الأساسية (مثل `Database.php`, `auth.php`, `WhatsAppService.php`).
- **`config/`**: يحتوي على ملفات الإعدادات (مثل `config.php`, `database.php`).
- **`database/`**: يحتوي على ملفات قاعدة البيانات (`tekrit_municipality.sql`) ومخططات الهيكلية.
- **`admin/`**: يبدو أنه يحتوي على ملفات قليلة، حيث أن معظم المنطق الإداري موجود في `modules/`.
- **`uploads/`**: مجلد لتخزين الملفات المرفقة (صور، مستندات).

### الملفات الجذرية:
- يحتوي المجلد الرئيسي على العديد من ملفات الإصلاح (`fix_...`) والاختبار (`test_...`)، مما يشير إلى عمليات صيانة وتطوير مستمرة.

## 3. الأقسام الداخلية (Backend - للموظفين)
يتم إدارة هذا القسم بشكل رئيسي من خلال الملفات الموجودة في `modules/`.

### الوحدات الأساسية:
1. **إدارة البلدية (`municipality_management.php`)**:
   - **الهيكل الإداري**: إدارة الأقسام والموظفين.
   - **اللجان**: إنشاء وإدارة اللجان البلدية (دائمة، مؤقتة).
   - **الجلسات**: جدولة وإدارة محاضر الجلسات.
   - **القرارات**: توثيق ومتابعة القرارات البلدية وحالات تنفيذها.

2. **إدارة الموارد البشرية (`hr.php`)**:
   - إدارة بيانات الموظفين، الرواتب، والحضور.

3. **الإدارة المالية (`finance.php`, `budgets.php`)**:
   - متابعة الميزانية، المصاريف، والإيرادات.

4. **إدارة المحتوى (`public_content_management.php`)**:
   - إدارة الأخبار، المشاريع، والإعلانات التي تظهر للمواطنين.

5. **إدارة المواطنين (`citizens.php`)**:
   - عرض بيانات المواطنين وطلباتهم.

### نظام الصلاحيات:
- يعتمد على نظام أدوار (Roles) وصلاحيات محددة لكل موظف (يتم التحقق منها عبر `includes/auth.php`).

## 4. الأقسام الخارجية (Frontend - للمواطنين)
يتم إدارة هذا القسم من خلال الملفات في `public/`.

### المميزات للمواطن:
1. **الحساب الشخصي (`citizen-dashboard.php`)**:
   - الدخول عبر "رمز دخول" (Access Code) أو رابط سحري (Magic Link).
   - عرض ملخص للطلبات والشكاوى والرسائل الواردة.

2. **تقديم الطلبات (`citizen-requests.php`)**:
   - نماذج لتقديم طلبات متنوعة (رخص، إفادات، إلخ).
   - نظام تتبع (`track-request.php`) باستخدام رقم تتبع (Tracking Number).

3. **الشكاوى (`citizen-complaints.php`)**:
   - تقديم شكاوى ومتابعة حالتها.

4. **المعلومات العامة**:
   - أخبار البلدية، المشاريع، ودليل الاتصال.

## 5. تحليل قاعدة البيانات (Database)
- **المحرك**: MySQL.
- **الملف الرئيسي**: `database/tekrit_municipality.sql`.
- **الاتصال**: يتم عبر `includes/Database.php` باستخدام PDO.

### الجداول الرئيسية (من خلال التحليل):
- `users`: بيانات الموظفين وصلاحياتهم.
- `citizens_accounts`: بيانات المواطنين (الاسم، الهاتف، الرمز).
- `citizen_requests`: الطلبات المقدمة من المواطنين.
- `complaints`: الشكاوى.
- `departments`: الأقسام الإدارية.
- `municipal_committees`: اللجان.
- `committee_sessions`: جلسات اللجان.
- `committee_decisions`: القرارات.
- `magic_links` & `citizen_sessions`: لإدارة دخول المواطنين.

### الميزات المتقدمة:
- **Triggers**: يوجد triggers لأتمتة المهام (مثل تسجيل الرسائل في `whatsapp_log`).
- **Stored Procedures**: إجراءات مخزنة للتنظيف الدوري والصيانة.

## 6. التفاصيل التقنية (Technical Stack)
- **اللغة**: PHP (Native) - لا يعتمد على إطار عمل ضخم مثل Laravel، بل بنية مخصصة.
- **التصميم**: TailwindCSS - مما يضمن واجهة عصرية ومتجاوبة (RTL supported).
- **قاعدة البيانات**: MySQL.
- **الأمان**:
  - حماية CSRF (`csrf_middleware.php`).
  - التحقق من المدخلات (Input Validation).
  - نظام صلاحيات للموظفين.
- **التكاملات**:
  - **WhatsApp**: لإرسال التنبيهات للمواطنين.
  - **Telegram**: يبدو أن هناك تكامل للإشعارات أيضاً.

## 7. ملاحظات عامة
- المشروع يحتوي على عدد كبير من ملفات "Fix" و "Test" في المجلد الرئيسي، مما قد يجعل التصفح صعباً قليلاً. يُنصح بتنظيف هذه الملفات أو نقلها لمجلد أرشيف.
- الكود يدمج بين المنطق (Logic) والعرض (View) في نفس الملفات في كثير من الأحيان، وهو نمط تقليدي في PHP ولكنه فعال للمشاريع المتوسطة.
