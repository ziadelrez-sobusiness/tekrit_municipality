# 📱 سيناريوهات التحقق من رقم الهاتف

## 🎯 الهدف
توضيح جميع الحالات الممكنة عند إدخال/تعديل رقم الهاتف، وكيف يتصرف النظام في كل حالة.

---

## 📋 السيناريوهات

### 1️⃣ **مواطن جديد - رقم جديد** 🆕
**الحالة:**
- المواطن **لم يدخل** رمز دخول (ضغط "تخطي")
- أدخل رقم هاتف **غير موجود** في قاعدة البيانات

**النتيجة:**
```
🆕 جديد
🆕 رقم جديد - سيتم إنشاء حساب لك
[حد أزرق]
زر "التالي" ✅ مفعّل
```

---

### 2️⃣ **مواطن مسجّل - رقمه الأصلي** ✅
**الحالة:**
- المواطن **أدخل** رمز دخول: `TKT-A3B7K`
- رقم الهاتف المحمّل: `03495685`
- **لم يعدّل** الرقم (بقي كما هو)

**النتيجة:**
```
✅ رقمك
✅ رقم هاتفك الحالي
[حد أخضر]
زر "التالي" ✅ مفعّل
```

---

### 3️⃣ **مواطن مسجّل - يريد تغيير رقمه إلى رقم متاح** 🔄
**الحالة:**
- المواطن **أدخل** رمز دخول: `TKT-A3B7K`
- رقمه الأصلي: `03495685`
- **عدّل** الرقم إلى: `03999999` (رقم جديد غير موجود)

**النتيجة:**
```
🔄 متاح
🔄 رقم جديد متاح - سيتم تحديث رقمك
[حد أزرق]
زر "التالي" ✅ مفعّل
```

**عند الضغط على "التالي":**
- يتم استدعاء `updateCitizenData()`
- يتم تحديث رقم الهاتف في قاعدة البيانات
- يتم استخدام الرقم الجديد في الطلب

---

### 4️⃣ **مواطن مسجّل - يحاول تغيير رقمه إلى رقم محجوز** ❌
**الحالة:**
- المواطن **أدخل** رمز دخول: `TKT-A3B7K`
- رقمه الأصلي: `03495685`
- **عدّل** الرقم إلى: `03670065` (رقم موجود لمواطن آخر)

**النتيجة:**
```
❌ محجوز
❌ هذا الرقم مسجّل مسبقاً لمواطن آخر. لا يمكن استخدامه.
[حد أحمر]
زر "التالي" ❌ معطّل
```

**لا يمكن المتابعة!**

---

### 5️⃣ **مواطن جديد - رقم محجوز** ❌
**الحالة:**
- المواطن **لم يدخل** رمز دخول (ضغط "تخطي")
- أدخل رقم هاتف **موجود** لمواطن آخر: `03670065`

**النتيجة:**
```
❌ محجوز
❌ هذا الرقم مسجّل مسبقاً لمواطن آخر. لا يمكن استخدامه.
[حد أحمر]
زر "التالي" ❌ معطّل
```

**لا يمكن المتابعة!**

---

## 🔄 المتغيرات المستخدمة

### `loadedAccessCode`
- `null` → مواطن جديد (تخطي)
- `'TKT-A3B7K'` → مواطن مسجّل (جلب بياناته)

### `originalPhone`
- `null` → مواطن جديد
- `'03495685'` → رقم الهاتف الأصلي عند جلب البيانات

### `phone` (المدخل)
- الرقم الذي كتبه المواطن في حقل الهاتف

---

## 📊 جدول القرارات

| loadedAccessCode | originalPhone | phone (مدخل) | data.is_owner | data.available | النتيجة |
|------------------|---------------|---------------|---------------|----------------|---------|
| `null` | `null` | `03999999` | `false` | `true` | 🆕 جديد ✅ |
| `null` | `null` | `03670065` | `false` | `false` | ❌ محجوز |
| `'TKT-XXX'` | `'03495685'` | `'03495685'` | `true` | `true` | ✅ رقمك |
| `'TKT-XXX'` | `'03495685'` | `'03999999'` | `false` | `true` | 🔄 متاح ✅ |
| `'TKT-XXX'` | `'03495685'` | `'03670065'` | `false` | `false` | ❌ محجوز |

---

## 🧪 اختبارات

### السيناريو 1: مواطن مسجّل - رقمه الأصلي
```
1. افتح: http://localhost:8080/tekrit_municipality/public/citizen-requests.php
2. أدخل رمز دخول: TKT-A3B7K
3. اضغط "جلب البيانات"
4. رقم الهاتف سيظهر: 03495685
5. انتظر 1 ثانية
6. النتيجة المتوقعة: ✅ رقمك | ✅ رقم هاتفك الحالي
```

### السيناريو 2: مواطن مسجّل - تغيير إلى رقم متاح
```
1. افتح: http://localhost:8080/tekrit_municipality/public/citizen-requests.php
2. أدخل رمز دخول: TKT-A3B7K
3. اضغط "جلب البيانات"
4. عدّل رقم الهاتف إلى: 03999999
5. النتيجة المتوقعة: 🔄 متاح | 🔄 رقم جديد متاح - سيتم تحديث رقمك
6. اضغط "التالي"
7. سيتم تحديث الرقم في قاعدة البيانات
```

### السيناريو 3: مواطن مسجّل - تغيير إلى رقم محجوز
```
1. افتح: http://localhost:8080/tekrit_municipality/public/citizen-requests.php
2. أدخل رمز دخول: TKT-A3B7K
3. اضغط "جلب البيانات"
4. عدّل رقم الهاتف إلى: 03670065 (رقم موجود لمواطن آخر)
5. النتيجة المتوقعة: ❌ محجوز | ❌ هذا الرقم مسجّل مسبقاً...
6. زر "التالي" معطّل ❌
```

---

## ✨ المزايا

✅ **فحص فوري** - بعد 1.5 ثانية من التوقف عن الكتابة  
✅ **رسائل واضحة** - 4 حالات مختلفة بألوان مميزة  
✅ **حماية كاملة** - لا يمكن استخدام رقم محجوز  
✅ **تحديث ذكي** - التحديث يحدث فقط للمواطنين المسجّلين  
✅ **تجربة مستخدم ممتازة** - تعطيل/تفعيل تلقائي لزر "التالي"

---

**تاريخ التحديث:** 2025-11-12  
**الحالة:** ✅ جاهز ومُختبر

